# Copy this file to .env and fill in real values before running
# docker-compose up. Never commit the actual .env file.

DB_NAME=clover_crm
DB_USER=clover_user
DB_PASSWORD=changeme-use-a-real-password

DJANGO_SECRET_KEY=changeme-generate-a-real-random-secret-key
DJANGO_DEBUG=False
DJANGO_ALLOWED_HOSTS=*
